workflow:                                                                  # New in GitLab 13.5
  rules:                                                                   # Only/except are considered legacy
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'

stages:
  - test

run_tests_job:
  image: docker-registry.ebrains.eu/workflow-components/gitlab-runner:podman
  tags:
    - docker-runner
  stage: test
  script:
    - cd tools/BluepyEfe/test
    - /opt/env/bin/cwltool --podman ../extract_ephys_features.cwl extract_ephys_features.yaml
    #- /opt/env/bin/python cwl_test.py
